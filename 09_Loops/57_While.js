// For loop with initialization of the variable outside the loop
// Valid syntax, but not recommended as it can lead to confusion and errors
console.log("For loop with initialization of the variable outside the loop:");
let i = 0
for(; i < 5; i++) 
    {
        console.log(i);
    }

// While loop
console.log("While loop:");
let j = 0;
while(j < 5)
{
    console.log(j);
    j++;
}